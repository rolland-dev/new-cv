{% extends 'base.html.twig' %}

{% block title %}login{% endblock %}


{% block body %}

    <h1>Connexion !</h1>

    <form action="{{ path('security_login') }}" method="POST">
        <div class="form-group" id="nom">
            <input placeholder="Adresse mail..." type="text" required name="_username" class="form-controle">
        </div>
        <div class="form-group">
            <input placeholder="Mot de passe..." type="password" required name="_password" class="form-controle">
        </div>

                    <div class="form-group">
                        <button type="submit btn btn-success">Connexion !</button>
                    </div>
    </form>   

    

{% endblock %}